# Contributing

Use synthetic CIDRs and disposable subscriptions. Run `./scripts/validate.sh`
and attach sanitized Bicep `what-if`, effective-route, firewall-policy, and
teardown evidence for infrastructure changes. Conventional Commits are
required. Never weaken the checked-in opt-in or deny defaults.
